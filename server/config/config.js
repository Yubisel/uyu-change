module.exports = {
    port: 3000,
    apiUrl: '/api-v1',

    /** set the format for log's 
     * to see examples visit 
     * https://www.npmjs.com/package/morgan
     * 
     * ej: dev, tiny, combined, common, short
     */
    logger: 'dev'
};